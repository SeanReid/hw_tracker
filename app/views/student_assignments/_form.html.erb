<%= form_for(@student_assignment) do |f| %>
  <% if @student_assignment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@student_assignment.errors.count, "error") %> prohibited this student_assignment from being saved:</h2>

      <ul>
      <% @student_assignment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :assignment_id %><br>
    <%= f.collection_select(:assignment_id, Assignment.all, :id, :title, prompt: true) %>
  </div>
  <div class="field">
    <%= f.label :student_id %><br>
    <%= f.select("student_id", Student.all.collect {|p| [ p.name, p.id ] }, {include_blank: 'None'}) %>
  </div>
  <div class="field">
    <%= f.label :on_time %><br>
    <%= f.text_field :on_time %>
  </div>
  <div class="field">
    <%= f.label :grade %><br>
    <%= f.text_field :grade %>
  </div>
  <div class="field">
    <%= f.label :github_url %><br>
    <%= f.text_field :github_url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
