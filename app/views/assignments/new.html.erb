<%= form_for @student_assignment, url: :assignment do |f| %>
  <% if @student_assignment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@student_assignment.errors.count, "NAHHHH") %> prohibited this completed_assignment from being saved:</h2>

      <ul>
      <% @student_assignment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <%= f.hidden_field :student_id %>
  <%= f.hidden_field :assignment_id %>
  <%= f.text_field :github_url %>
  <%= f.submit "Submit" %>
<% end %>
